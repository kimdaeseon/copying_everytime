{{>layout/header}}

<h1 style="text-align: center">{{board}}</h1>
<div class="main-center" style="text-align: center">
    <div class="col-md-12">
        <!----
    <div class="row">
        <div class="col-md-6">
            <a href="/posts/save" role="button" class="btn btn-primary"> 글등록 </a>
            {{#userName}}
                Logged in as: <span id="user">{{userName}}</span>
                <a href="/logout" class="btn btn-info active" role="button">Logout</a>
            {{/userName}}
            {{^userName}}
                <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
                <a href="/oauth2/authorization/naver" class="btn btn-success active" role="button">Naver Login</a>
            {{/userName}}
        </div>
    </div>
    <br>
    -->
        <div class="row">
            <a href="/{{board}}/post/save" role="button" class="btn btn-primary"> 글등록 </a>
        </div>
        <br>
        <!---목록 출력 영역 -->
        <table class="table table-horizontal table-bordered">
            <thead class="thead-strong">
            <tr>
                <th>제목</th>
                <th>작성자</th>
                <th>최종수정일</th>
            </tr>
            </thead>
            <tbody id="tbody">
            {{#post}}
                <tr>
                    <td><a href="/posts/update/{{id}}">{{title}}</a></td>
                    <td>{{author}}</td>
                    <td>{{modifiedDate}}</td>
                </tr>
            {{/post}}
            </tbody>
        </table>
        {{#number_of_post}}
            <a href="/boards/{{board}}/{{.}}">{{.}}</a>
        {{/number_of_post}}
    </div>
</div>


{{>layout/footer}}
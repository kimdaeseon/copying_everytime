{{>layout/header}}

<h1>시간표 등록</h1>

<div class="col-md-12">
    <div class="col-md-4">
        <form>
            <div class="form-group">
                <label for="schedule">게시판 이름</label>
                <input type="text" class="form-control" id="schedule_name" placeholder="내용을 입력하세요">
            </div>
        </form>
        <a href="/" role="button" class="btn btn-secondary">취소</a>
        <button type="button" class="btn btn-primary" id="btn-schedule-save">등록</button>
    </div>
</div>

{{>layout/footer}}